
1. Define experiment configurations in `experiments.csv`

2. Run all experiments
    ```bash
    ./run.sh
    ```
    You will be prompted to choose which notebook to run (baseline or VQEL).

3. View results 

    All experiment outputs—including logs, metrics, and notebook result—are saved in `experiments/` directory.
